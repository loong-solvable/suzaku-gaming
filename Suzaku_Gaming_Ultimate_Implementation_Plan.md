# Suzaku Gaming 后台管理系统 - 终极架构与实施白皮书

---

## 0. 文档元信息

### 0.1 版本信息

| 属性 | 值 |
|------|-----|
| 文档版本 | v1.0.0 |
| 生成日期 | 2026-02-03 |
| 作者 | AI Architect |
| 适用范围 | Suzaku Gaming 后台管理系统前端复刻项目 |

### 0.2 关联文档清单

| 文档 | 代号 | 用途 |
|------|------|------|
| `网站复刻报告.md` | SSoT | 唯一规格事实源 |
| `Decision_Log.md` | DL | 冲突裁决记录 |
| `Traceability_Matrix.md` | TM | 需求追踪矩阵 |
| `Test_Plan_and_Cases.md` | TP | 测试计划与用例 |
| `Visual_Spec_and_Tokens.md` | VS | 视觉标注与Token |

### 0.3 变更历史

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0.0 | 2026-02-03 | 初始版本，融合SSoT+方案A+方案B | AI Architect |

---

## 1. 项目目标与范围

### 1.1 目标（量化）

| 目标项 | 量化指标 | 验收方式 |
|--------|----------|----------|
| 视觉还原度 | 像素误差 ≤1px | pixelmatch对比 |
| 功能完整度 | 100%覆盖SSoT定义的功能 | 追踪矩阵验证 |
| 代码质量 | ESLint 0错误，TypeScript严格模式 | CI门禁 |
| 单测覆盖率 | ≥80% | Vitest Coverage |
| 构建体积 | ≤500KB (gzip) | Vite构建输出 |
| 首屏性能 | FCP ≤1.5s, LCP ≤2.5s | Lighthouse |

### 1.2 包含范围（逐条列）

1. **全局布局**
   - 侧边栏组件（展开/折叠）
   - 顶部导航栏（面包屑、用户信息、操作按钮）
   - 主内容区（路由视图容器）

2. **页面实现**
   - 概要面板（Dashboard）：12个KPI卡片
   - 角色列表：10个筛选字段 + 14列表格 + 分页 + 排序 + 导出
   - 订单列表：11个筛选字段 + 14列表格 + 累计金额 + 分页 + 导出
   - 绑定申请：6个筛选字段 + 7列表格 + 状态标签 + 操作按钮
   - 新增归因更改：7个表单字段 + 文件上传

3. **通用组件**
   - StatCard（统计卡片）
   - FilterBar（筛选表单）
   - DataTable（数据表格）
   - ImageUpload（图片上传）
   - Pagination（分页）

4. **工程基础**
   - Mock数据服务
   - Axios封装（拦截器、错误处理、重试）
   - Pinia状态管理
   - 路由配置

5. **测试覆盖**
   - 单元测试（Vitest）
   - E2E测试（Playwright）
   - 视觉回归测试（pixelmatch）

### 1.3 不包含范围（逐条列）

1. ❌ 真实后端API与数据库持久化
2. ❌ 用户登录认证与权限系统实现（仅UI展示）
3. ❌ 多语言国际化（固定简体中文）
4. ❌ 深色主题切换
5. ❌ 图表与复杂数据可视化（仅实现数字卡片）
6. ❌ 移动端适配（最小支持1024px）
7. ❌ SSR服务端渲染
8. ❌ 生产环境CI/CD部署配置

### 1.4 交付形态

| 交付物 | 格式 | 说明 |
|--------|------|------|
| 前端源码 | Git仓库 | 可直接`pnpm install && pnpm dev`运行 |
| 构建产物 | 静态文件 | 可部署到任意静态服务器 |
| 文档 | Markdown | README + 本实施白皮书 |
| 测试报告 | HTML | Vitest + Playwright报告 |

---

## 2. 成功标准与验收口径（全部量化）

### 2.1 视觉验收

| 验收项 | 标准 | 测量方法 |
|--------|------|----------|
| 基准分辨率 | 1920×1080 | 浏览器设置 |
| 缩放比例 | 100% | 浏览器设置 |
| 叠图方法 | Playwright截图 + pixelmatch | 自动化脚本 |
| 误差阈值 | ≤1px | pixelmatch差异像素 |
| 颜色误差 | 0（必须精确匹配Token） | 开发者工具取色 |
| 字体误差 | 字号±0px，行高±0px | 开发者工具检查 |

### 2.2 功能验收

| 功能点 | 验收标准 | 测试用例ID |
|--------|----------|-----------|
| 筛选功能 | 所有字段可输入/选择，查询返回正确数据 | TC-R-01~10 |
| 清空功能 | 点击后所有筛选条件重置为空 | TC-R-12 |
| 分页功能 | 切换页码数据正确，切换pageSize重置到第1页 | TC-P-01~06 |
| 排序功能 | 点击列头升序/降序切换，数据正确排序 | TC-R-22~26 |
| 导出功能 | 生成CSV文件，UTF-8 BOM编码，列顺序与表格一致 | TC-R-13 |
| 上传功能 | 支持jpg/png，≤500KB，≤10个，拖拽/点击均可 | TC-N-08~22 |
| 表单校验 | 必填项为空显示红色错误提示 | TC-N-01~06 |
| 状态覆盖 | 正常/hover/active/disabled/loading/empty/error 均已实现 | 全部E2E用例 |

### 2.3 工程质量验收

| 验收项 | 标准 | 命令 |
|--------|------|------|
| 构建无警告 | `pnpm build` 0 warnings | `pnpm build` |
| ESLint通过 | 0 errors, 0 warnings | `pnpm lint` |
| Stylelint通过 | 0 errors | `pnpm lint:style` |
| TypeScript通过 | 0 errors | `pnpm type-check` |
| 单测覆盖率 | ≥80% | `pnpm test:coverage` |
| E2E通过 | 100%用例通过 | `pnpm test:e2e` |

### 2.4 性能验收

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| FCP (First Contentful Paint) | ≤1.5s | Chrome Lighthouse |
| LCP (Largest Contentful Paint) | ≤2.5s | Chrome Lighthouse |
| TTI (Time to Interactive) | ≤3.0s | Chrome Lighthouse |
| Bundle Size (gzip) | ≤500KB | `pnpm build` 输出 |
| 路由切换 | ≤300ms | Performance面板 |

**测量条件**：
- Chrome最新版，桌面端
- Fast 3G模拟
- 禁用缓存

---

## 3. 假设与约束

### 3.1 浏览器兼容性

| 浏览器 | 最低版本 | 支持程度 |
|--------|----------|----------|
| Chrome | 120+ | 完整支持 |
| Edge | 120+ | 完整支持 |
| Firefox | 88+ | 完整支持 |
| Safari | 14+ | 完整支持 |
| IE | - | 不支持 |

### 3.2 分辨率范围

| 断点 | 宽度范围 | 布局行为 |
|------|----------|----------|
| XL | ≥1440px | 默认布局，侧边栏220px |
| LG | 1280px-1439px | 默认布局，表格横向滚动 |
| MD | 1024px-1279px | 侧边栏自动折叠为64px |
| SM | <1024px | 显示"请使用桌面设备访问"提示 |

### 3.3 其他假设

| 假设项 | 说明 |
|--------|------|
| 语言 | 固定简体中文，不考虑国际化 |
| 数据来源 | 全部使用Mock.js模拟，无真实后端 |
| 权限 | 展示管理员角色，权限系统仅UI展示 |
| 用户 | 目标用户为桌面端运营人员 |

---

## 4. 总体架构

### 4.1 技术栈（版本锁定）

| 层级 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 运行时 | Node.js | 20.11.1 | 开发环境 |
| 包管理 | pnpm | ^8.0.0 | 依赖管理 |
| 前端框架 | Vue | 3.4.21 | UI框架 |
| 开发语言 | TypeScript | ^5.0.0 | 类型安全 |
| 构建工具 | Vite | 5.2.8 | 开发构建 |
| UI组件库 | Element Plus | 2.6.1 | 企业级组件 |
| 路由 | Vue Router | 4.3.0 | SPA路由 |
| 状态管理 | Pinia | 2.1.7 | 状态管理 |
| HTTP客户端 | Axios | ^1.6.0 | 请求封装 |
| 日期处理 | dayjs | 1.11.10 | 日期格式化 |
| 样式预处理 | Sass | 1.69.5 | CSS预处理 |
| Mock服务 | mockjs | 1.1.0 | 数据模拟 |
| 代码规范 | ESLint | 8.57.0 | 代码检查 |
| 样式规范 | Stylelint | 16.2.1 | 样式检查 |
| 格式化 | Prettier | 3.2.5 | 代码格式化 |
| 单元测试 | Vitest | 1.5.0 | 单元测试 |
| 组件测试 | @vue/test-utils | 2.4.5 | Vue组件测试 |
| E2E测试 | Playwright | 1.41.0 | 端到端测试 |
| 视觉对比 | pixelmatch | 5.3.0 | 像素对比 |

### 4.2 目录结构（完整树形）

```
suzaku-gaming-admin/
├── .vscode/                          # VS Code 配置
│   ├── settings.json                 # 编辑器设置
│   └── extensions.json               # 推荐扩展
├── e2e/                              # E2E测试
│   ├── dashboard.spec.ts
│   ├── role-list.spec.ts
│   ├── order-list.spec.ts
│   ├── binding-apply.spec.ts
│   ├── new-attribution.spec.ts
│   └── visual/                       # 视觉回归测试
│       ├── baseline/                 # 基准截图
│       └── pages.visual.spec.ts
├── public/                           # 静态资源（不参与构建）
│   └── favicon.ico
├── src/
│   ├── api/                          # API接口层
│   │   ├── index.ts                  # API统一导出
│   │   ├── dashboard.ts              # Dashboard接口
│   │   ├── player.ts                 # 玩家数据接口
│   │   └── audit.ts                  # 审核模块接口
│   ├── assets/                       # 静态资源（参与构建）
│   │   └── styles/                   # 全局样式
│   │       ├── tokens.scss           # 设计Token
│   │       ├── variables.scss        # SCSS变量
│   │       ├── element.scss          # Element Plus覆盖
│   │       ├── reset.scss            # 样式重置
│   │       └── index.scss            # 样式入口
│   ├── components/                   # 通用组件
│   │   ├── layout/                   # 布局组件
│   │   │   ├── Sidebar/
│   │   │   │   ├── index.vue
│   │   │   │   └── MenuItem.vue
│   │   │   ├── Header/
│   │   │   │   ├── index.vue
│   │   │   │   ├── Breadcrumb.vue
│   │   │   │   └── UserInfo.vue
│   │   │   └── AppMain/
│   │   │       └── index.vue
│   │   └── common/                   # 业务通用组件
│   │       ├── StatCard/
│   │       │   ├── index.vue
│   │       │   └── __tests__/
│   │       │       └── StatCard.spec.ts
│   │       ├── FilterBar/
│   │       │   ├── index.vue
│   │       │   └── __tests__/
│   │       │       └── FilterBar.spec.ts
│   │       ├── DataTable/
│   │       │   ├── index.vue
│   │       │   └── __tests__/
│   │       │       └── DataTable.spec.ts
│   │       └── ImageUpload/
│   │           ├── index.vue
│   │           └── __tests__/
│   │               └── ImageUpload.spec.ts
│   ├── composables/                  # 组合式函数
│   │   ├── useTable.ts               # 表格逻辑
│   │   ├── useFilter.ts              # 筛选逻辑
│   │   └── useSubmitLock.ts          # 防重复提交
│   ├── layouts/                      # 布局组件
│   │   └── MainLayout.vue
│   ├── mock/                         # Mock数据
│   │   ├── index.ts                  # Mock入口
│   │   ├── dashboard.ts              # Dashboard Mock
│   │   ├── player.ts                 # 玩家数据Mock
│   │   └── audit.ts                  # 审核模块Mock
│   ├── router/                       # 路由配置
│   │   ├── index.ts                  # 路由入口
│   │   └── routes.ts                 # 路由定义
│   ├── stores/                       # Pinia状态管理
│   │   ├── index.ts                  # Store入口
│   │   ├── app.ts                    # 应用状态
│   │   └── user.ts                   # 用户状态
│   ├── types/                        # TypeScript类型
│   │   ├── api.d.ts                  # API类型
│   │   ├── components.d.ts           # 组件类型
│   │   └── global.d.ts               # 全局类型
│   ├── utils/                        # 工具函数
│   │   ├── request.ts                # Axios封装
│   │   ├── format.ts                 # 格式化工具
│   │   ├── export.ts                 # 导出工具
│   │   └── storage.ts                # 本地存储
│   ├── views/                        # 页面组件
│   │   ├── Dashboard/
│   │   │   └── index.vue
│   │   ├── PlayerData/
│   │   │   ├── RoleList.vue
│   │   │   └── OrderList.vue
│   │   └── Audit/
│   │       ├── BindingApply.vue
│   │       └── NewAttribution.vue
│   ├── App.vue                       # 根组件
│   └── main.ts                       # 应用入口
├── .env                              # 环境变量
├── .env.development                  # 开发环境变量
├── .env.production                   # 生产环境变量
├── .eslintrc.cjs                     # ESLint配置
├── .prettierrc                       # Prettier配置
├── .stylelintrc.cjs                  # Stylelint配置
├── .nvmrc                            # Node版本
├── index.html                        # HTML入口
├── package.json                      # 项目配置
├── pnpm-lock.yaml                    # 依赖锁定
├── tsconfig.json                     # TypeScript配置
├── tsconfig.node.json                # Node TypeScript配置
├── vite.config.ts                    # Vite配置
├── vitest.config.ts                  # Vitest配置
├── playwright.config.ts              # Playwright配置
└── README.md                         # 项目说明
```

### 4.3 模块划分

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                Application                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │   Views     │  │  Components │  │   Router    │  │   Stores    │       │
│  │  (页面组件)  │  │  (通用组件)  │  │   (路由)    │  │  (状态管理)  │       │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘       │
│         │                │                │                │               │
│         └────────────────┴────────────────┴────────────────┘               │
│                                   │                                         │
│                         ┌─────────▼─────────┐                              │
│                         │    Composables    │                              │
│                         │   (组合式函数)     │                              │
│                         └─────────┬─────────┘                              │
│                                   │                                         │
│         ┌─────────────────────────┼─────────────────────────┐              │
│         │                         │                         │              │
│  ┌──────▼──────┐          ┌───────▼───────┐         ┌───────▼───────┐     │
│  │     API     │          │    Utils      │         │    Types      │     │
│  │   (接口层)   │          │  (工具函数)   │         │  (类型定义)   │     │
│  └──────┬──────┘          └───────────────┘         └───────────────┘     │
│         │                                                                   │
│  ┌──────▼──────┐                                                           │
│  │    Mock     │                                                           │
│  │  (数据模拟)  │                                                           │
│  └─────────────┘                                                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.4 路由树（与SSoT完全一致）

```typescript
// src/router/routes.ts
import type { RouteRecordRaw } from 'vue-router';
import MainLayout from '@/layouts/MainLayout.vue';

export const routes: RouteRecordRaw[] = [
  {
    path: '/',
    component: MainLayout,
    redirect: '/dashboard',
    children: [
      {
        path: 'dashboard',
        name: 'Dashboard',
        component: () => import('@/views/Dashboard/index.vue'),
        meta: { 
          title: '概要面板', 
          icon: 'House',
          breadcrumb: ['概要面板']
        }
      },
      {
        path: 'player-data',
        name: 'PlayerData',
        redirect: '/player-data/role-list',
        meta: { 
          title: '玩家数据报表', 
          icon: 'DataLine' 
        },
        children: [
          {
            path: 'role-list',
            name: 'RoleList',
            component: () => import('@/views/PlayerData/RoleList.vue'),
            meta: { 
              title: '角色列表',
              breadcrumb: ['玩家数据报表', '角色列表']
            }
          },
          {
            path: 'order-list',
            name: 'OrderList',
            component: () => import('@/views/PlayerData/OrderList.vue'),
            meta: { 
              title: '订单列表',
              breadcrumb: ['玩家数据报表', '订单列表']
            }
          }
        ]
      },
      {
        path: 'audit',
        name: 'Audit',
        redirect: '/audit/binding-apply',
        meta: { 
          title: '审核', 
          icon: 'Checked' 
        },
        children: [
          {
            path: 'binding-apply',
            name: 'BindingApply',
            component: () => import('@/views/Audit/BindingApply.vue'),
            meta: { 
              title: '绑定申请',
              breadcrumb: ['审核', '绑定申请']
            }
          },
          {
            path: 'new-attribution',
            name: 'NewAttribution',
            component: () => import('@/views/Audit/NewAttribution.vue'),
            meta: { 
              title: '新增归因更改',
              breadcrumb: ['审核', '新增归因更改']
            }
          }
        ]
      }
    ]
  }
];
```

### 4.5 布局结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            MainLayout                                        │
├──────────────┬──────────────────────────────────────────────────────────────┤
│              │                        Header (56px)                          │
│              │  ┌────────────────────────────────────────────────────────┐  │
│   Sidebar    │  │ [≡] 玩家数据报表 > 角色列表           [🖨️] [👤 管理员 ▼] │  │
│   (220px)    │  └────────────────────────────────────────────────────────┘  │
│              ├──────────────────────────────────────────────────────────────┤
│  ┌────────┐  │                        AppMain                                │
│  │ Logo   │  │  ┌────────────────────────────────────────────────────────┐  │
│  ├────────┤  │  │                                                        │  │
│  │ 概要   │  │  │                    <router-view />                     │  │
│  │ 面板   │  │  │                                                        │  │
│  ├────────┤  │  │                    (页面内容区)                         │  │
│  │ 玩家   │  │  │                                                        │  │
│  │ 数据 ▼ │  │  │                    padding: 16px                       │  │
│  │  ├角色 │  │  │                    background: #F0F2F5                 │  │
│  │  └订单 │  │  │                    min-height: calc(100vh - 56px)      │  │
│  ├────────┤  │  │                                                        │  │
│  │ 审核 ▼ │  │  │                                                        │  │
│  │  └绑定 │  │  └────────────────────────────────────────────────────────┘  │
│  └────────┘  │                                                              │
└──────────────┴──────────────────────────────────────────────────────────────┘
```

### 4.6 面包屑生成策略

```typescript
// src/components/layout/Header/Breadcrumb.vue
import { computed } from 'vue';
import { useRoute } from 'vue-router';

const route = useRoute();

// 从路由meta中获取面包屑配置
const breadcrumbs = computed(() => {
  return route.meta.breadcrumb as string[] || [];
});

// 渲染面包屑
// 分隔符: >
// 最后一项不可点击
// 前面的项可点击跳转
```

---

## 5. 设计系统（Design System）

### 5.1 视觉标注流程

```
设计稿/截图 → Figma测量 → visual-spec.md → tokens.scss → 组件实现 → 测试baseline
```

**变更铁律**：任何视觉变更必须先更新`Visual_Spec_and_Tokens.md`，再同步修改代码。

### 5.2 颜色Token表（完整Hex值）

| Token名称 | Hex值 | 用途 |
|-----------|-------|------|
| `--color-primary` | #409EFF | 主色调 |
| `--color-success` | #67C23A | 成功状态 |
| `--color-warning` | #E6A23C | 警告状态 |
| `--color-danger` | #F56C6C | 危险状态 |
| `--color-info` | #909399 | 信息状态 |
| `--sidebar-bg` | #1e222d | 侧边栏背景 |
| `--sidebar-active-bg` | #409EFF | 激活菜单背景 |
| `--bg-page` | #F0F2F5 | 页面背景 |
| `--bg-card` | #FFFFFF | 卡片背景 |
| `--text-primary` | #303133 | 主要文字 |
| `--text-regular` | #606266 | 常规文字 |
| `--text-secondary` | #909399 | 次要文字 |
| `--text-placeholder` | #C0C4CC | 占位符 |
| `--border-color` | #E4E7ED | 边框色 |

详细Token表见`Visual_Spec_and_Tokens.md`。

### 5.3 Element Plus变量映射

```scss
:root {
  --el-color-primary: #409EFF;
  --el-color-success: #67C23A;
  --el-color-warning: #E6A23C;
  --el-color-danger: #F56C6C;
  --el-text-color-primary: #303133;
  --el-text-color-regular: #606266;
  --el-border-color: #E4E7ED;
  --el-bg-color: #FFFFFF;
  --el-menu-bg-color: #1e222d;
  --el-menu-text-color: #FFFFFF;
}
```

### 5.4 字体规范

| 场景 | 字号 | 字重 | 行高 | 颜色 |
|------|------|------|------|------|
| 页面标题 | 20px | 600 | 28px | #303133 |
| 卡片标题 | 14px | 400 | 22px | #303133 |
| 卡片数值 | 28px | 700 | 36px | #303133 |
| 表头文字 | 14px | 600 | 22px | #606266 |
| 表格内容 | 14px | 400 | 22px | #303133 |
| 按钮文字 | 14px | 400 | 22px | - |
| 菜单项 | 14px | 400 | 22px | #FFFFFF |
| Logo | 16px | 600 | 24px | #FFFFFF |

字体族：`-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Microsoft YaHei', sans-serif`

### 5.5 间距与尺寸阶梯

| Token | 值 | 用途 |
|-------|-----|------|
| `--spacing-xs` | 4px | 最小间距 |
| `--spacing-sm` | 8px | 小间距 |
| `--spacing-md` | 12px | 中间距 |
| `--spacing-base` | 16px | 基础间距 |
| `--spacing-lg` | 20px | 大间距 |
| `--spacing-xl` | 24px | 超大间距 |

**布局尺寸**：
- 侧边栏展开：220px
- 侧边栏收起：64px
- 顶部栏：56px
- 内容区内边距：16px

### 5.6 圆角与阴影

| 场景 | 圆角 | 阴影 |
|------|------|------|
| 按钮 | 4px | 无 |
| 输入框 | 4px | 无 |
| 卡片 | 8px | `0 2px 12px rgba(0,0,0,0.1)` |
| 下拉面板 | 4px | `0 2px 12px rgba(0,0,0,0.12)` |
| 弹窗 | 8px | `0 8px 24px rgba(0,0,0,0.15)` |

### 5.7 组件状态定义

| 状态 | 按钮表现 | 输入框表现 | 表格行表现 |
|------|----------|-----------|-----------|
| 默认 | 正常颜色 | 边框#DCDFE6 | 白色背景 |
| 悬停 | 颜色加深10% | 边框#C0C4CC | 背景#F5F7FA |
| 激活 | 颜色加深20% | 边框#409EFF | - |
| 禁用 | 透明度50% | 背景#F5F7FA | - |
| 加载 | 显示spinner | - | 遮罩+spinner |
| 空态 | - | - | 显示el-empty |
| 错误 | - | 边框#F56C6C | - |

### 5.8 资源清单

| 资源 | 规格 |
|------|------|
| Logo | 文本"Suzaku Gaming"，16px/600，白色 |
| 头像 | 32px×32px，圆形，默认背景#D9D9D9 |
| 空状态图 | el-empty默认，64px×64px |
| 上传占位 | 100%×150px，虚线边框#DCDFE6 |
| 缩略图 | 100px×100px，圆角4px |

---

## 6. 页面实现规格（逐页详述）

### 6.1 概要面板（Dashboard）

#### 布局规格

| 属性 | 值 |
|------|-----|
| 布局方式 | Grid 4列×3行 |
| 卡片间距 | 20px (el-row gutter=20) |
| 卡片高度 | 120px |
| 卡片圆角 | 8px |
| 卡片阴影 | `0 2px 12px rgba(0,0,0,0.1)` |
| 卡片内边距 | 20px |

#### 卡片规格

```
┌─────────────────────────────┐
│  今日新增                    │  ← 标题：#303133，14px
│  2026-02-02 周一             │  ← 日期：#909399，12px
│                             │
│     286                     │  ← 数值：#303133，28px，700
│                         人  │  ← 单位：#909399，12px
└─────────────────────────────┘
```

#### 12个指标数据（Mock值）

| 行 | 指标1 | 指标2 | 指标3 | 指标4 |
|----|-------|-------|-------|-------|
| 今日 | 今日新增：286人 | 今日活跃：1035人 | 今日付费：126人 | 今日金额：1050.35 USD |
| 当月 | 当月新增：8036人 | 当月活跃：20350人 | 当月付费：3150人 | 当月金额：30150.98 USD |
| 历史 | 历史新增：206350人 | 历史活跃：806350人 | 历史付费：10350人 | 历史金额：150350.98 USD |

#### 状态处理

| 状态 | 处理方式 |
|------|----------|
| 加载中 | 整体v-loading遮罩 |
| 加载失败 | ElMessage.error("请求失败，请重试") |
| 数据为空 | 显示0（不会出现空态） |

### 6.2 角色列表

#### 筛选区域

| 字段 | 类型 | 宽度 | 占位符 | 接口参数 |
|------|------|------|--------|----------|
| 游戏项目 | Select | 200px | 请选择游戏项目 | project |
| 区服 | Select | 200px | 请选择区服 | server |
| 一级渠道 | Select | 200px | 请选择一级渠道 | channel1 |
| 二级渠道 | Select | 200px | 请选择二级渠道 | channel2 |
| 三级渠道 | Select | 200px | 请选择三级渠道 | channel3 |
| 系统 | Select | 200px | 请选择系统 | system |
| 时区 | Select | 200px | 请选择时区 | timezone |
| 角色ID | Input | 200px | 请输入角色ID | roleId |
| 角色昵称 | Input | 200px | 请输入角色昵称 | nickname |
| 注册时间 | DateRange | 320px | 开始日期 至 结束日期 | startDate, endDate |

#### 操作按钮

| 按钮 | 类型 | 图标 | 颜色 |
|------|------|------|------|
| 查询 | Primary | Search | #409EFF |
| 清空 | Default | Delete | 白底灰边 |
| 导出 | Success | Download | #67C23A |

#### 表格列

| 列名 | 字段 | 宽度 | 可排序 | 对齐 |
|------|------|------|--------|------|
| 项目 | project | 120px | 否 | 左 |
| 角色ID | roleId | 150px | 否 | 左 |
| UCID | ucid | 150px | 否 | 左 |
| 区服 | server | 100px | 否 | 左 |
| 系统 | system | 80px | 否 | 左 |
| 角色昵称 | nickname | 120px | 否 | 左 |
| 国家 | country | 60px | 否 | 左 |
| 角色等级 | level | 80px | 否 | 右 |
| 注册时间 | registerTime | 160px | 是 | 左 |
| 最后登录时间 | lastLoginTime | 160px | 是 | 左 |
| 最后更改时间 | lastUpdateTime | 160px | 是 | 左 |
| 总付费金额 | totalPayment | 120px | 是 | 右 |
| 总付费笔数 | paymentCount | 100px | 是 | 右 |
| 一级渠道 | channel1 | 100px | 否 | 左 |

#### 分页配置

| 属性 | 值 |
|------|-----|
| 默认每页 | 20条 |
| 选项 | [10, 20, 50, 100] |
| 布局 | total, sizes, prev, pager, next, jumper |
| 位置 | 右对齐，margin-top: 16px |

#### 导出规格

| 属性 | 值 |
|------|-----|
| 格式 | CSV (UTF-8 BOM) |
| 文件名 | `角色列表_{YYYYMMDD}_{HHmmss}.csv` |
| 列顺序 | 与表格一致 |
| 数据范围 | 当前筛选条件全量数据 |

#### 状态处理

| 状态 | 处理方式 |
|------|----------|
| 加载中 | 表格区域v-loading |
| 空数据 | el-empty "暂无数据" |
| 查询失败 | ElMessage.error |

### 6.3 订单列表

#### 筛选字段

与角色列表类似，增加：
- 订单类型（Select）

#### 累计金额显示

```html
<div class="total-amount">
  累计充值金额：<span style="color: #F56C6C; font-weight: 700;">4150.98</span>
</div>
```

位置：表格上方左侧，margin-bottom: 8px

#### 表格列

| 列名 | 字段 | 宽度 | 可排序 | 对齐 |
|------|------|------|--------|------|
| 项目 | project | 120px | 否 | 左 |
| 角色ID | roleId | 150px | 否 | 左 |
| 区服 | server | 100px | 否 | 左 |
| 系统 | system | 80px | 否 | 左 |
| 角色昵称 | nickname | 120px | 否 | 左 |
| 角色等级 | level | 80px | 否 | 右 |
| 充值时间 | payTime | 160px | 是 | 左 |
| 最后登录时间 | lastLoginTime | 160px | 是 | 左 |
| 充值金额 | amount | 100px | 是 | 右 |
| 币种 | currency | 60px | 否 | 左 |
| 订单类型 | orderType | 100px | 否 | 左 |
| 订单号 | orderNo | 200px | 否 | 左 |
| 充值渠道 | payChannel | 100px | 否 | 左 |
| 一级渠道 | channel1 | 100px | 否 | 左 |

### 6.4 绑定申请

#### 筛选字段

| 字段 | 类型 | 宽度 | 接口参数 |
|------|------|------|----------|
| 游戏项目 | Select | 200px | project |
| 区服 | Select | 200px | server |
| 角色ID | Input | 200px | roleId |
| 申请人 | Input | 200px | applicant |
| 审核状态 | Select | 200px | status |
| 申请时间 | DateRange | 320px | startDate, endDate |

#### 特殊按钮

- **归因修改申请**：type="primary" plain，点击跳转到`/audit/new-attribution`

#### 表格列

| 列名 | 字段 | 宽度 | 固定 | 说明 |
|------|------|------|------|------|
| ID | id | 80px | - | - |
| 游戏项目 | project | 120px | - | - |
| 角色ID | roleId | 150px | - | - |
| 区服 | server | 100px | - | - |
| 申请人 | applicant | 100px | - | - |
| 状态 | status | 100px | - | el-tag渲染 |
| 申请时间 | applyTime | 160px | - | - |
| 操作 | - | 180px | right | 查看/编辑/删除 |

#### 状态标签映射

| 状态值 | 显示文本 | el-tag类型 | 背景色 | 文字色 |
|--------|----------|-----------|--------|--------|
| pending | 未审核 | info | #ECF5FF | #409EFF |
| approved | 审核通过 | success | #E1F3D8 | #67C23A |
| rejected | 审核拒绝 | danger | #FEF0F0 | #F56C6C |

#### 操作按钮

| 按钮 | 类型 | 功能 |
|------|------|------|
| 查看 | type="primary" link | 弹窗显示详情 |
| 编辑 | type="success" link | 弹窗可编辑 |
| 删除 | type="danger" link | 确认后删除 |

### 6.5 归因更改表单

#### 表单配置

| 属性 | 值 |
|------|-----|
| 标签宽度 | 120px |
| 标签位置 | right |
| 尺寸 | default |

#### 表单字段

| 字段 | 类型 | 必填 | 校验规则 | 错误提示 |
|------|------|------|----------|----------|
| 角色ID | Input | 是 | required | 请输入角色ID |
| 区服 | Select | 是 | required | 请选择区服 |
| 角色昵称 | Input | 是 | required, max:20 | 请输入角色昵称 |
| 一级平台 | Select | 是 | required | 请选择一级平台 |
| 组长 | Select | 是 | required | 请选择组长 |
| 组员 | Select | 是 | required | 请选择组员 |
| 附件 | Upload | 否 | - | - |

#### 文件上传规格

| 属性 | 值 |
|------|-----|
| 支持格式 | .jpg, .jpeg, .png |
| 单文件大小 | ≤500KB |
| 最大数量 | 10个 |
| 拖拽上传 | 支持 |
| 预览尺寸 | 100px × 100px |
| 预览圆角 | 4px |

#### 操作按钮

| 按钮 | 类型 | 功能 |
|------|------|------|
| 提交 | Primary | 表单校验 → 提交 → 成功提示 |
| 取消 | Default | 返回绑定申请页 |

---

## 7. 组件与复用设计

### 7.1 StatCard

```vue
<template>
  <div class="stat-card">
    <div class="stat-card__header">
      <span class="stat-card__title">{{ title }}</span>
    </div>
    <div class="stat-card__date">{{ dateRange }}</div>
    <div class="stat-card__content">
      <span class="stat-card__value">{{ formattedValue }}</span>
      <span class="stat-card__unit">{{ unit }}</span>
    </div>
  </div>
</template>

<script setup lang="ts">
interface Props {
  title: string;
  dateRange: string;
  value: number;
  unit: string;
}

const props = defineProps<Props>();

const formattedValue = computed(() => {
  if (props.unit === 'USD') {
    return props.value.toLocaleString('en-US', { 
      minimumFractionDigits: 2,
      maximumFractionDigits: 2 
    });
  }
  return props.value.toLocaleString();
});
</script>

<style lang="scss" scoped>
.stat-card {
  background: var(--bg-card);
  border-radius: 8px;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
  padding: 20px;
  height: 120px;
  
  &__title {
    font-size: 14px;
    color: var(--text-primary);
  }
  
  &__date {
    font-size: 12px;
    color: var(--text-secondary);
    margin-top: 4px;
  }
  
  &__content {
    margin-top: 16px;
    display: flex;
    align-items: baseline;
  }
  
  &__value {
    font-size: 28px;
    font-weight: 700;
    color: var(--text-primary);
  }
  
  &__unit {
    font-size: 12px;
    color: var(--text-secondary);
    margin-left: 4px;
  }
}
</style>
```

### 7.2 FilterBar

**Props**:
- `fields: FilterField[]` - 字段配置
- `modelValue: Record<string, any>` - 表单值
- `showExport?: boolean` - 是否显示导出按钮

**Emits**:
- `update:modelValue` - 值更新
- `search` - 搜索
- `reset` - 重置
- `export` - 导出

### 7.3 DataTable

**Props**:
- `columns: TableColumn[]` - 列配置
- `data: any[]` - 表格数据
- `loading?: boolean` - 加载状态
- `pagination?: PaginationConfig` - 分页配置
- `stripe?: boolean` - 斑马纹（默认true）

**Emits**:
- `sort-change` - 排序变化
- `page-change` - 页码变化
- `size-change` - 每页条数变化

**固定列配置**:
- 操作列: `fixed: 'right'`
- 固定列阴影: 自动添加

**空状态**:
- 数据为空时显示el-empty
- 描述文字: "暂无数据"

**加载状态**:
- 使用v-loading指令
- 遮罩背景: rgba(255,255,255,0.9)

### 7.4 Pagination

**配置**:
```vue
<el-pagination
  v-model:current-page="page"
  v-model:page-size="pageSize"
  :total="total"
  :page-sizes="[10, 20, 50, 100]"
  layout="total, sizes, prev, pager, next, jumper"
  @current-change="handlePageChange"
  @size-change="handleSizeChange"
/>
```

### 7.5 ImageUpload

**Props**:
- `limit: number` - 最大数量（默认10）
- `maxSize: number` - 最大大小（默认512000，即500KB）
- `accept: string` - 接受格式（默认".jpg,.jpeg,.png"）
- `modelValue: FileItem[]` - 已上传文件列表

**错误处理**:
- 格式错误: "只支持 jpg/png 格式的图片"
- 大小错误: "文件大小不能超过 500KB"
- 数量错误: "最多只能上传 10 个文件"
- 上传失败: "文件上传失败，请重试"

### 7.6 Dialogs

**查看弹窗**:
- 只读展示数据
- 关闭按钮

**编辑弹窗**:
- 表单可编辑
- 保存/取消按钮
- 表单校验

**删除确认**:
- 使用ElMessageBox.confirm
- 文案: "确认删除该记录？"
- 确认后调用删除API

### 7.7 Message（消息提示规范）

| 场景 | 类型 | 文案 | 时长 |
|------|------|------|------|
| 保存成功 | success | 保存成功 | 3000ms |
| 删除成功 | success | 删除成功 | 3000ms |
| 导出成功 | success | 导出成功，文件已开始下载 | 3000ms |
| 网络错误 | error | 网络连接失败，请检查网络后重试 | 5000ms |
| 服务器错误 | error | 服务器繁忙，请稍后重试 | 5000ms |
| 表单校验失败 | warning | 请检查必填项是否填写完整 | 5000ms |

---

## 8. 状态管理（Pinia）

### 8.1 useAppStore

```typescript
// src/stores/app.ts
import { defineStore } from 'pinia';

interface AppState {
  sidebarCollapsed: boolean;
  loading: boolean;
  networkStatus: 'online' | 'offline';
}

export const useAppStore = defineStore('app', {
  state: (): AppState => ({
    sidebarCollapsed: localStorage.getItem('sidebarCollapsed') === 'true',
    loading: false,
    networkStatus: 'online'
  }),
  
  getters: {
    sidebarWidth(): number {
      return this.sidebarCollapsed ? 64 : 220;
    }
  },
  
  actions: {
    toggleSidebar() {
      this.sidebarCollapsed = !this.sidebarCollapsed;
      localStorage.setItem('sidebarCollapsed', String(this.sidebarCollapsed));
    },
    
    setLoading(loading: boolean) {
      this.loading = loading;
    },
    
    setNetworkStatus(status: 'online' | 'offline') {
      this.networkStatus = status;
    }
  }
});
```

### 8.2 useUserStore

```typescript
// src/stores/user.ts
import { defineStore } from 'pinia';

interface UserInfo {
  id: number;
  username: string;
  realName: string;
  role: string;
  avatar?: string;
}

interface UserState {
  userInfo: UserInfo | null;
}

export const useUserStore = defineStore('user', {
  state: (): UserState => ({
    userInfo: {
      id: 1,
      username: '3kadmin',
      realName: '管理员',
      role: 'admin',
      avatar: ''
    }
  }),
  
  getters: {
    isLoggedIn(): boolean {
      return this.userInfo !== null;
    },
    
    displayName(): string {
      return this.userInfo?.realName || this.userInfo?.username || '';
    }
  }
});
```

### 8.3 持久化策略

| Key | 存储位置 | 内容 |
|-----|----------|------|
| `sidebarCollapsed` | localStorage | 侧边栏折叠状态 |

---

## 9. 数据与接口契约

### 9.1 通用响应格式（成功）

```typescript
interface ApiSuccessResponse<T> {
  code: 0;
  message: 'success';
  data: T;
  timestamp: number;
}
```

### 9.2 通用响应格式（错误）

```typescript
interface ApiErrorResponse {
  code: number;  // 非0
  message: string;
  timestamp: number;
}
```

### 9.3 分页响应格式

```typescript
interface PaginatedResponse<T> {
  code: 0;
  message: 'success';
  data: {
    list: T[];
    pagination: {
      page: number;
      pageSize: number;
      total: number;
      totalPages: number;
    };
  };
  timestamp: number;
}
```

### 9.4 接口清单

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| Dashboard统计 | GET | /api/dashboard/statistics | 获取12个KPI |
| 角色列表 | GET | /api/player/roles | 分页+筛选+排序 |
| 订单列表 | GET | /api/player/orders | 分页+筛选+排序 |
| 绑定申请列表 | GET | /api/audit/binding-applies | 分页+筛选 |
| 新增绑定申请 | POST | /api/audit/binding-applies | 创建申请 |
| 更新绑定申请 | PUT | /api/audit/binding-applies/:id | 更新状态 |
| 删除绑定申请 | DELETE | /api/audit/binding-applies/:id | 删除记录 |

### 9.5 数据模型（TypeScript Interface）

```typescript
// 角色
interface Character {
  id: number;
  project: string;
  roleId: string;
  ucid: string;
  server: string;
  system: 'iOS' | 'Android';
  nickname: string;
  country: string;
  level: number;
  registerTime: string;
  lastLoginTime: string;
  lastUpdateTime: string;
  totalPayment: number;
  paymentCount: number;
  channel1: string;
}

// 订单
interface Order {
  id: number;
  project: string;
  roleId: string;
  server: string;
  system: string;
  nickname: string;
  level: number;
  payTime: string;
  lastLoginTime: string;
  amount: number;
  currency: string;
  orderType: string;
  orderNo: string;
  payChannel: string;
  channel1: string;
}

// 绑定申请
interface BindingApply {
  id: number;
  project: string;
  roleId: string;
  server: string;
  applicant: string;
  status: 'pending' | 'approved' | 'rejected';
  applyTime: string;
  remark?: string;
}
```

### 9.6 Mock数据规则

**使用真实数值**（禁止"张三/李四"等无意义数据）：

- 今日新增：286人
- 今日活跃：1035人
- 今日付费玩家：126人
- 今日付费金额：1050.35 USD
- 当月新增玩家：8036人
- 当月活跃玩家：20350人
- 当月付费玩家：3150人
- 当月付费金额：30150.98 USD
- 历史新增用户：206350人
- 历史活跃用户：806350人
- 历史付费用户：10350人
- 历史付费金额：150350.98 USD

---

## 10. 核心Hooks源码

### 10.1 useTable

```typescript
// src/composables/useTable.ts
import { ref, reactive, onUnmounted } from 'vue';

interface UseTableOptions<T> {
  fetchApi: (params: any) => Promise<{ list: T[]; total: number }>;
  defaultPageSize?: number;
  immediate?: boolean;
}

export function useTable<T = any>(options: UseTableOptions<T>) {
  const { fetchApi, defaultPageSize = 20, immediate = true } = options;
  
  const loading = ref(false);
  const data = ref<T[]>([]);
  const pagination = reactive({
    page: 1,
    pageSize: defaultPageSize,
    total: 0
  });
  const sort = reactive({
    prop: null as string | null,
    order: null as 'ascending' | 'descending' | null
  });
  
  let abortController: AbortController | null = null;
  
  const fetchData = async (filterParams: Record<string, any> = {}) => {
    if (abortController) {
      abortController.abort();
    }
    abortController = new AbortController();
    
    loading.value = true;
    try {
      const params = {
        page: pagination.page,
        pageSize: pagination.pageSize,
        sortBy: sort.prop,
        sortOrder: sort.order === 'ascending' ? 'asc' : 
                   sort.order === 'descending' ? 'desc' : null,
        ...filterParams
      };
      
      const result = await fetchApi(params);
      data.value = result.list;
      pagination.total = result.total;
    } catch (error: any) {
      if (error.name !== 'AbortError') {
        console.error('Table fetch error:', error);
      }
    } finally {
      loading.value = false;
    }
  };
  
  const handlePageChange = (page: number) => {
    pagination.page = page;
  };
  
  const handleSizeChange = (size: number) => {
    pagination.pageSize = size;
    pagination.page = 1;
  };
  
  const handleSortChange = ({ prop, order }: { prop: string | null; order: 'ascending' | 'descending' | null }) => {
    sort.prop = prop;
    sort.order = order;
    pagination.page = 1;
  };
  
  const reset = () => {
    pagination.page = 1;
    pagination.pageSize = defaultPageSize;
    sort.prop = null;
    sort.order = null;
  };
  
  onUnmounted(() => {
    if (abortController) {
      abortController.abort();
    }
  });
  
  return {
    loading,
    data,
    pagination,
    sort,
    fetchData,
    handlePageChange,
    handleSizeChange,
    handleSortChange,
    reset
  };
}
```

### 10.2 useFilter

```typescript
// src/composables/useFilter.ts
import { reactive } from 'vue';

interface UseFilterOptions<T> {
  defaultValues: T;
  onSearch?: (values: T) => void;
  onReset?: () => void;
}

export function useFilter<T extends Record<string, any>>(options: UseFilterOptions<T>) {
  const { defaultValues, onSearch, onReset } = options;
  
  const filterValues = reactive<T>(JSON.parse(JSON.stringify(defaultValues)));
  
  const handleSearch = () => {
    onSearch?.(filterValues as T);
  };
  
  const handleReset = () => {
    Object.keys(defaultValues).forEach(key => {
      (filterValues as any)[key] = (defaultValues as any)[key];
    });
    onReset?.();
  };
  
  const getFilterValues = (): T => {
    return JSON.parse(JSON.stringify(filterValues));
  };
  
  return {
    filterValues,
    handleSearch,
    handleReset,
    getFilterValues
  };
}
```

### 10.3 useSubmitLock

```typescript
// src/composables/useSubmitLock.ts
import { ref } from 'vue';

export function useSubmitLock() {
  const isSubmitting = ref(false);
  
  const withLock = async <T>(fn: () => Promise<T>): Promise<T | null> => {
    if (isSubmitting.value) {
      console.warn('重复提交已被阻止');
      return null;
    }
    
    isSubmitting.value = true;
    try {
      return await fn();
    } finally {
      isSubmitting.value = false;
    }
  };
  
  return {
    isSubmitting,
    withLock
  };
}
```

---

## 11. 网络层封装

### 11.1 Axios实例配置

```typescript
// src/utils/request.ts
import axios from 'axios';
import type { AxiosInstance, AxiosRequestConfig, AxiosResponse, AxiosError } from 'axios';
import { ElMessage, ElMessageBox } from 'element-plus';

const service: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_APP_BASE_API,
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
});
```

### 11.2 请求拦截器

```typescript
service.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);
```

### 11.3 响应拦截器（含错误处理）

```typescript
service.interceptors.response.use(
  (response: AxiosResponse) => {
    const { code, message, data } = response.data;
    
    if (code === 0) {
      return data;
    }
    
    ElMessage.error(message || '请求失败');
    return Promise.reject(new Error(message));
  },
  (error: AxiosError) => {
    if (!error.response) {
      if (error.code === 'ECONNABORTED') {
        ElMessage.error('请求超时，请重试');
      } else {
        ElMessage.error('网络连接失败，请检查网络');
      }
      return Promise.reject(error);
    }
    
    const status = error.response.status;
    const errorMap: Record<number, string> = {
      400: '请求参数错误',
      401: '登录已过期，请重新登录',
      403: '没有权限访问',
      404: '请求的资源不存在',
      500: '服务器繁忙，请稍后重试',
      502: '网关错误',
      503: '服务暂时不可用'
    };
    
    ElMessage.error(errorMap[status] || `请求失败 (${status})`);
    
    return Promise.reject(error);
  }
);
```

### 11.4 请求重试机制

```typescript
export const requestWithRetry = async <T>(
  config: AxiosRequestConfig,
  retries = 3,
  delay = 1000
): Promise<T> => {
  for (let i = 0; i < retries; i++) {
    try {
      return await service(config);
    } catch (error) {
      if (i === retries - 1) throw error;
      await new Promise(resolve => setTimeout(resolve, delay));
    }
  }
  throw new Error('Max retries exceeded');
};
```

### 11.5 请求取消（AbortController）

```typescript
// 使用示例
const controller = new AbortController();
const response = await service.get('/api/data', { 
  signal: controller.signal 
});

// 取消请求
controller.abort();
```

---

## 12. 工程规范

### 12.1 目录结构规范

- 组件目录使用`PascalCase`
- 页面文件使用`PascalCase`
- 工具函数使用`camelCase`
- 样式文件使用`kebab-case`

### 12.2 文件命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| Vue组件 | PascalCase | `StatCard.vue` |
| 组合式函数 | use前缀 + camelCase | `useTable.ts` |
| 工具函数 | camelCase | `formatDate.ts` |
| 类型定义 | camelCase + .d.ts | `api.d.ts` |
| 常量 | UPPER_SNAKE_CASE | `API_TIMEOUT` |

### 12.3 代码规范（ESLint/Stylelint/Prettier配置）

**ESLint规则**:
- 使用`@vue/eslint-config-typescript`
- 禁止`any`滥用
- 强制使用`const`/`let`
- 要求函数返回类型

**Stylelint规则**:
- 使用`stylelint-config-standard-scss`
- 属性排序
- 颜色格式小写

**Prettier配置**:
```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "none",
  "printWidth": 100
}
```

### 12.4 TypeScript策略

- 严格模式：`"strict": true`
- 禁止隐式any：`"noImplicitAny": true`
- 空值检查：`"strictNullChecks": true`
- Props必须有类型定义
- 函数参数和返回值必须有类型

### 12.5 Git分支策略

| 分支 | 用途 | 命名规则 |
|------|------|----------|
| main | 主分支 | 固定 |
| develop | 开发分支 | 固定 |
| feature/* | 功能分支 | `feature/功能名` |
| bugfix/* | 修复分支 | `bugfix/问题描述` |

### 12.6 Commit规范（Conventional Commits）

```
<type>(<scope>): <subject>

<body>

<footer>
```

| Type | 说明 |
|------|------|
| feat | 新功能 |
| fix | Bug修复 |
| style | 样式修改 |
| refactor | 重构 |
| docs | 文档 |
| chore | 构建/工具 |

### 12.7 PR审查门禁

- [ ] ESLint 0 errors
- [ ] TypeScript类型检查通过
- [ ] 单元测试通过
- [ ] 无`console.log`残留
- [ ] 无`any`类型滥用

---

## 13. 测试方案

### 13.1 单元测试（Vitest）

- 框架：Vitest + @vue/test-utils
- 覆盖率要求：≥80%
- 目标：公共组件、Hooks、工具函数

### 13.2 E2E测试（Playwright）

- 框架：Playwright
- 浏览器：Chromium
- 分辨率：1920×1080
- 覆盖：5个页面主路径+异常路径

### 13.3 视觉回归测试（Playwright + pixelmatch）

- 基准截图目录：`e2e/visual/baseline/`
- 差异阈值：1px
- 测试5个关键页面

### 13.4 覆盖率要求与运行方式

```bash
# 单元测试
pnpm test

# 单元测试（带覆盖率）
pnpm test:coverage

# E2E测试
pnpm test:e2e

# 视觉回归测试
pnpm test:visual

# 全部测试
pnpm test:all
```

详细测试用例见`Test_Plan_and_Cases.md`。

---

## 14. 构建与发布

### 14.1 脚本命令清单

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx --fix",
    "lint:style": "stylelint \"**/*.{vue,scss,css}\" --fix",
    "format": "prettier --write src/",
    "type-check": "vue-tsc --noEmit",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:visual": "playwright test e2e/visual/",
    "test:all": "pnpm test:coverage && pnpm test:e2e"
  }
}
```

### 14.2 环境变量配置

```bash
# .env.development
VITE_APP_TITLE=Suzaku Gaming Admin
VITE_APP_BASE_API=/api
VITE_APP_MOCK=true

# .env.production
VITE_APP_TITLE=Suzaku Gaming Admin
VITE_APP_BASE_API=https://api.suzaku.com
VITE_APP_MOCK=false
```

### 14.3 Vite配置

```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import { resolve } from 'path';

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  css: {
    preprocessorOptions: {
      scss: {
        additionalData: `@import "@/assets/styles/variables.scss";`
      }
    }
  },
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'element-plus': ['element-plus'],
          'vendor': ['vue', 'vue-router', 'pinia']
        }
      }
    }
  }
});
```

### 14.4 静态部署方式

1. 执行`pnpm build`
2. 将`dist/`目录内容部署到静态服务器
3. 配置服务器将所有路由指向`index.html`（SPA模式）

---

## 15. 安全规格

### 15.1 XSS防护

- Vue自动转义：默认启用
- 禁止v-html渲染用户输入
- 禁止innerHTML赋值用户输入

### 15.2 代码禁止项

```typescript
// ❌ 禁止
v-html="userInput"
eval(userInput)
innerHTML = userInput
localStorage.setItem('password', pwd)
new Function(userInput)

// ✅ 允许
{{ userInput }}  // 自动转义
```

### 15.3 敏感信息处理

- 不存储真实用户密码
- 不在前端代码中硬编码密钥
- 生产环境禁止暴露堆栈信息

---

## 16. 里程碑与WBS

### 16.1 里程碑定义

| 里程碑 | 内容 | 验收标准 |
|--------|------|----------|
| M1 | 项目初始化、布局框架 | `pnpm dev`可运行 |
| M2 | 公共组件、Dashboard | 12卡片正确显示 |
| M3 | 列表页、审核页 | 筛选/分页/排序可用 |
| M4 | 像素对齐、测试验收 | 全部测试通过 |

### 16.2 阶段任务分解

见`实施计划书.md`第三章详细任务分解。

### 16.3 任务优先级标记

| 优先级 | 标识 | 含义 |
|--------|------|------|
| P0 | 🔴 | 核心功能，必须完成 |
| P1 | 🟡 | 重要功能，应该完成 |
| P2 | 🟢 | 增强功能，最好完成 |

### 16.4 每项任务的验收产物

| 阶段 | 验收产物 |
|------|----------|
| Phase 1 | 可运行框架、配置文件、工具函数 |
| Phase 2 | 布局组件、路由配置、状态管理 |
| Phase 3 | Dashboard、列表页、通用组件 |
| Phase 4 | 表单页、弹窗、导出功能 |
| Phase 5 | 测试报告、构建产物 |

---

## 17. 风险与对策

### 17.1 风险清单

| 风险 | 概率 | 影响 | 优先级 |
|------|------|------|--------|
| 截图尺寸不确定 | 中 | 中 | P1 |
| Element Plus样式不一致 | 中 | 中 | P1 |
| 复杂表格性能问题 | 低 | 中 | P2 |
| 需求理解偏差 | 中 | 高 | P0 |

### 17.2 对策与回滚方案

| 风险 | 对策 | 回滚方案 |
|------|------|----------|
| 截图尺寸不确定 | 以visual-spec为唯一参考 | - |
| Element Plus样式不一致 | tokens.scss显式覆盖 | 回退到默认样式 |
| 性能问题 | 虚拟滚动、懒加载 | 简化功能 |
| 需求偏差 | 每阶段交付确认 | git reset |

---

## 18. 变更管理

### 18.1 visual-spec更新流程

```
设计变更请求 → 更新Visual_Spec_and_Tokens.md → 更新tokens.scss → 更新组件 → 更新baseline
```

### 18.2 tokens更新流程

```
修改tokens.scss → 检查Element Plus变量映射 → 验证组件样式 → 提交PR
```

### 18.3 Decision_Log维护规则

- 任何冲突裁决必须记录
- 格式按`Decision_Log.md`模板
- 每条裁决必须有理由

### 18.4 Changelog规范

```markdown
## [版本号] - 日期

### Added
- 新功能

### Changed
- 变更

### Fixed
- 修复
```

---

## 附录

### A. 完整验收清单（可打印）

#### 布局检查
- [ ] 侧边栏展开宽度220px
- [ ] 侧边栏收起宽度64px
- [ ] 侧边栏背景色#1e222d
- [ ] 顶部栏高度56px
- [ ] 内容区内边距16px
- [ ] 页面背景色#F0F2F5

#### Dashboard页面
- [ ] 12个卡片正确显示
- [ ] Grid 4列×3行布局
- [ ] 卡片间距20px
- [ ] 数值千分位格式化
- [ ] 加载状态正确

#### 列表页面
- [ ] 所有筛选字段可用
- [ ] 查询/清空/导出按钮可用
- [ ] 表格斑马纹显示
- [ ] 排序功能正常
- [ ] 分页功能正常
- [ ] 空状态显示正确

#### 表单页面
- [ ] 必填字段红色星号
- [ ] 表单校验提示正确
- [ ] 文件上传功能正常
- [ ] 提交/取消按钮可用

#### 交互效果
- [ ] 菜单切换高亮
- [ ] 按钮悬停效果
- [ ] Loading状态
- [ ] 消息提示

### B. 环境配置速查

```bash
# Node版本
nvm use 20.11.1

# 安装依赖
pnpm install

# 开发
pnpm dev

# 构建
pnpm build

# 测试
pnpm test:all
```

### C. 命令速查

| 命令 | 用途 |
|------|------|
| `pnpm dev` | 开发服务器 |
| `pnpm build` | 生产构建 |
| `pnpm preview` | 预览构建 |
| `pnpm lint` | 代码检查 |
| `pnpm format` | 代码格式化 |
| `pnpm type-check` | 类型检查 |
| `pnpm test` | 单元测试 |
| `pnpm test:coverage` | 覆盖率测试 |
| `pnpm test:e2e` | E2E测试 |

### D. 图标映射表

| 功能 | 图标名称 | Element Plus |
|------|----------|--------------|
| 概要面板 | House | @element-plus/icons-vue |
| 玩家数据报表 | DataLine | @element-plus/icons-vue |
| 审核 | Checked | @element-plus/icons-vue |
| 查询 | Search | @element-plus/icons-vue |
| 清空 | Delete | @element-plus/icons-vue |
| 导出 | Download | @element-plus/icons-vue |
| 打印 | Printer | @element-plus/icons-vue |
| 展开 | ArrowDown | @element-plus/icons-vue |
| 收起 | ArrowUp | @element-plus/icons-vue |

---

**文档版本**：v1.0.0  
**生成时间**：2026-02-03  
**架构师签章**：AI Architect

---

> 本文档为终极架构与实施白皮书，任何实施偏离必须获得项目负责人书面批准。
